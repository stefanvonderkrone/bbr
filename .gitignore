# Zig
zig-out/
.zig-cache/
zig-pkg/

# Local secrets — never commit. Source into your shell: set -a; source .env; set +a
.env
test.sh
run-bbr
bbr-submit-response.log

# Editor / OS
.DS_Store
.opencode
