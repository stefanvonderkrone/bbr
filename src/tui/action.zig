pub const Action = enum {
    down,
    up,
    half_page_down,
    half_page_up,
    page_down,
    page_up,
    to_top,
    to_bottom,
    center,
    scroll_cursor_top,
    scroll_cursor_bottom,
    cursor_view_top,
    cursor_view_middle,
    cursor_view_bottom,
    select_down,
    select_up,
    quit,
    open_picker,
    refresh,
    comment,
    inline_comment,
    suggest,
    reply,
    submit,
    recover_submission,
    resolve_unpublished,
    link_existing_comment,
    help,
    toggle_select,
    clear_selection,
    toggle_layout,
    cycle_scope,
    isolate,
    next_file,
    prev_file,
    toggle_disclosure,
};
